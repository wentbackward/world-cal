# ─────────────────────────────────────────────────────────
# Caddy Reverse Proxy Configuration
# ─────────────────────────────────────────────────────────
#
# Each subdomain routes to a container on the app-net network.
# Add new apps by adding a block below.
#
# Existing containers (test/prod) are routed by their ports.
# Update the port numbers to match your existing setup.
# ─────────────────────────────────────────────────────────

# ── Subdomain Apps ──────────────────────────────────────
# Each app runs as its own container on the app-net network.
# Caddy discovers them by container name.

world-cal.paulgresham.com {
    reverse_proxy world-cal:80
}

# Add new apps here:
# another-app.paulgresham.com {
#     reverse_proxy another-app:80
# }

# ── Existing Containers ─────────────────────────────────
# Route to your existing test/prod containers by port.
# Replace the port numbers with your actual ports.

# Example: test environment on port 3000
# test.paulgresham.com {
#     reverse_proxy localhost:3000
# }

# Example: production on port 8080
# paulgresham.com {
#     reverse_proxy localhost:8080
# }

# ── Global Settings ─────────────────────────────────────
# Uncomment to enable global compression and security headers
# {
#     auto_https off  # Disable if you want manual HTTPS control
# }
